{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"SMACC","text":"<p>Sleep Manipulation And Communication Clickything \u2014 a clickable interface for running sleep-related experiments.</p> <ul> <li>Trigger audio cues</li> <li>Collect dream reports</li> <li>Trigger EEG portcodes</li> <li>Save a detailed event log</li> <li>and more!</li> </ul>"},{"location":"#get-started","title":"Get started","text":"<ul> <li>Installation \u2014 download and run SMACC.</li> <li>Usage \u2014 trigger cues, record dream reports, send portcodes, and more.</li> <li>Contributing \u2014 develop SMACC and build these docs locally.</li> </ul> <p>SMACC is free software released under the GPL-3.0-or-later license.</p>"},{"location":"contributing/","title":"Contributing","text":"<p>This page is the canonical development guide for SMACC. It is written for human contributors and is also where AI coding assistants are pointed (see <code>AGENTS.md</code>), so the instructions live here once rather than being duplicated across files.</p>"},{"location":"contributing/#conventions","title":"Conventions","text":"<ul> <li>Always use uv to run Python scripts and install   dependencies. Never <code>pip install</code> or run naked <code>python</code>.</li> <li>Prefer shell commands over PowerShell commands.</li> </ul>"},{"location":"contributing/#development","title":"Development","text":"<pre><code>uv sync --extra dev        # create the environment with dev tools\nuv run python -m smacc     # launch the app\nuv run pytest              # run the test suite\nuv run ruff check .        # lint\nuv run ruff format .       # format\nuv run mypy                # type-check\npre-commit install         # enable the lint/format/type-check git hooks\n</code></pre> <p><code>uv run smacc</code> also works once the environment is synced \u2014 it's the installed GUI launcher and opens no console window, so prefer <code>python -m smacc</code> when you want terminal output (e.g. tracebacks).</p>"},{"location":"contributing/#building-the-executable","title":"Building the executable","text":"<p>Build the standalone Windows executable:</p> <pre><code>uv run pyinstaller entry.py --name SMACC --onefile --noconsole \\\n  --icon src/smacc/assets/icon.ico \\\n  --add-data \"src/smacc/assets/icon.png:smacc/assets\" \\\n  --add-data \"src/smacc/assets/cues:smacc/assets/cues\"\n</code></pre> <p><code>--icon</code> sets the executable's file icon; <code>--add-data</code> bundles the runtime window/taskbar icon, which SMACC resolves via <code>sys._MEIPASS</code>. On Windows the <code>--add-data</code> separator is <code>;</code> rather than <code>:</code> (i.e. <code>...png;smacc/assets</code>).</p> <p>PyYAML (settings export/import) is pure Python and is picked up automatically by PyInstaller; if a frozen build ever fails to import <code>yaml</code>, add <code>--hidden-import yaml</code>.</p> <p>Releases are built automatically: pushing a <code>v*</code> tag (e.g. <code>v0.0.7</code>) triggers the release workflow, which builds <code>SMACC.exe</code> and attaches it to the GitHub Release.</p>"},{"location":"contributing/#building-the-docs","title":"Building the docs","text":"<p>The documentation site is built with MkDocs + Material and versioned with mike. All pages are plain Markdown under <code>docs/</code>.</p> <pre><code>uv run --extra docs mkdocs serve          # live-reload preview at http://localhost:8000\nuv run --extra docs mkdocs build --strict # the exact build the PR CI runs\nuv run --extra docs mike serve            # preview the versioned site\n</code></pre> <p>The docs workflow runs <code>mkdocs build --strict</code> on every pull request, deploys a <code>dev</code> version on pushes to <code>main</code>, and publishes a numbered version (updating the <code>latest</code> alias) on each <code>v*</code> release tag.</p>"},{"location":"contributing/#project-notes","title":"Project notes","text":"<ul> <li><code>src/</code> layout: the package lives in <code>src/smacc/</code>.</li> <li>The single source of truth for the version is <code>__version__</code> in   <code>src/smacc/__init__.py</code>; <code>config.py</code> and the packaging metadata both read from   it.</li> </ul>"},{"location":"installation/","title":"Installation","text":"<p>To install SMACC, go to the releases page, click the Assets dropdown for the latest release, and download the SMACC.exe file. Once downloaded, double-clicking this file will run SMACC.</p> <p>Administrator privileges</p> <p>For some features you will need to open SMACC with Administrator privileges (right-click the file and select Run as administrator).</p>"},{"location":"installation/#optional-setup","title":"Optional setup","text":""},{"location":"installation/#data-directory","title":"Data directory","text":"<p>By default SMACC stores everything under <code>~/SMACC</code>. To use a different location, set an environment variable called <code>SMACC_DATA_DIRECTORY</code> to whatever directory you want. SMACC will create it and all of the subfolders (if not already present).</p> <p>Each run gets its own timestamped folder under <code>~/SMACC/sessions/</code> (e.g. <code>smacc-20260607-223015/</code>) holding that run's <code>.log</code>, dream-report recordings, and any exports. Subject/session are optional metadata (set from File \u203a Session info\u2026) recorded inside the log/exports rather than in filenames.</p>"},{"location":"installation/#audio-cues","title":"Audio cues","text":"<p>SMACC seeds a few <code>demo-*</code> cue files into <code>~/SMACC/cues</code> on first launch (restored if you delete them), so there is always something to test with. You can also place your own sound files there \u2014 <code>.wav</code>, <code>.mp3</code>, <code>.flac</code>, <code>.ogg</code>, and <code>.aiff</code> are all supported.</p>"},{"location":"installation/#dream-report-survey","title":"Dream report survey","text":"<p>The Record Dream Report button can optionally pop open a survey URL \u2014 for example a dream-report survey hosted on Qualtrics or REDCap. Add surveys from the Dream-recording panel's Manage\u2026 button (each has a name and a URL); they are saved to your settings YAML and restored next session. Pick one from the survey dropdown to open it automatically when recording starts, or open any saved survey on its own from File \u203a Surveys.</p>"},{"location":"installation/#recording-device","title":"Recording device","text":"<p>If you plan to record dreams, choose the input device from the SMACC menubar: Audio \u203a Input device \u203a [choose device].</p>"},{"location":"usage/","title":"Usage","text":"<p>SMACC presents a single clickable window for running sleep-related experiments. This page walks through the main features.</p>"},{"location":"usage/#audio-cues","title":"Audio cues","text":"<p>Place sound files in <code>~/SMACC/cues</code> (<code>.wav</code>, <code>.mp3</code>, <code>.flac</code>, <code>.ogg</code>, and <code>.aiff</code> are supported) and trigger them from the cue controls. SMACC ships a few <code>demo-*</code> cues so there is always something to test with.</p>"},{"location":"usage/#dream-reports","title":"Dream reports","text":"<p>Use the Record Dream Report button to record from the selected input device. Choose the device from Audio \u203a Input device \u203a [choose device]. Recordings are saved into the current session folder.</p> <p>Surveys (e.g. a dream-report questionnaire on Qualtrics or REDCap) open in your browser. Manage your saved surveys with the Manage\u2026 button next to the survey dropdown \u2014 each has a name and URL and is stored in your settings YAML. Select one in the dropdown to open it automatically when recording starts, or open any saved survey on its own from File \u203a Surveys (each open is logged as a <code>SurveyOpened</code> event).</p>"},{"location":"usage/#eeg-portcodes","title":"EEG portcodes","text":"<p>SMACC can trigger EEG portcodes to mark events in your recording, keeping cue delivery and your neural data in sync.</p>"},{"location":"usage/#event-log","title":"Event log","text":"<p>Every run writes a detailed <code>.log</code> to its own timestamped folder under <code>~/SMACC/sessions/</code>, capturing the events and settings for that session.</p>"},{"location":"usage/#settings-yaml","title":"Settings (YAML)","text":"<p>Save the current setup with File \u203a Export settings (YAML)\u2026 and reload it later with File \u203a Load settings (YAML)\u2026. You can also pull the initial/final settings back out of a <code>.log</code> with File \u203a Load settings from log\u2026.</p>"}]}